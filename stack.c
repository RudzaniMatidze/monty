#include "monty.h"
/**
 * f_stack - prints the stack at the top
 * @head: stack head
 * @counter: line_number counter
 * Return: void
 */
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
